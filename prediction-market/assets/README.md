# assets/
**Version 1.0.0 · Last updated 2026-07-22**

Static files: images and fonts.

The site currently uses no bundled image. The logo is inline SVG in
`app/components/Shell.tsx`. The fonts are system families declared in
`tailwind.config.js`.

Put a real asset here when one is needed. Do not put an asset in the root.
